const sum = 2 + 4;
const diff = 10 - 5;
const multiply = 20 * 556;
const division = 10 / 2;
const module = 24 % 5;
const power = Math.pow(5, 2);
const squareRoot = Math.sqrt(9);

console.log(sum, diff, multiply, division, module, power, squareRoot);
